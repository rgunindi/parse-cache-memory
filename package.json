{
  "name": "parse-cache-memory",
  "version": "2.1.1",
  "description": "In-memory cache implementation for Parse Server",
  "main": "index.js",
  "scripts": {
    "pretest:unit": "node scripts/cleanup.js",
    "pretest:integration": "node scripts/cleanup.js",
    "pretest:main": "node scripts/cleanup.js",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules PARSE_MASTER_KEY=test-master-key PARSE_APP_ID=test-app-id jest --testTimeout=60000 --runInBand --forceExit --detectOpenHandles --no-coverage",
    "test:unit": "cross-env NODE_OPTIONS=--experimental-vm-modules PARSE_MASTER_KEY=test-master-key PARSE_APP_ID=test-app-id jest --testTimeout=60000 --runInBand --forceExit --detectOpenHandles --no-coverage __tests__/unit/parse-cache-memory.test.js",
    "test:integration": "cross-env NODE_OPTIONS=--experimental-vm-modules PARSE_MASTER_KEY=test-master-key PARSE_APP_ID=test-app-id jest --testTimeout=60000 --runInBand --forceExit --detectOpenHandles __tests__/integration/cache-integration.test.js",
    "test:main": "cross-env NODE_OPTIONS=--experimental-vm-modules PARSE_MASTER_KEY=test-master-key PARSE_APP_ID=test-app-id jest --testTimeout=60000 --runInBand --forceExit --detectOpenHandles __tests__/parse-cache-memory.test.js",
    "test:watch": "jest --watch --testTimeout=60000 --runInBand",
    "test:coverage": "jest --coverage --testTimeout=60000 --runInBand --forceExit",
    "lint": "eslint . --fix",
    "format": "prettier --write \"**/*.{js,json,md}\""
  },
  "keywords": [
    "parse",
    "cache",
    "memory",
    "caching"
  ],
  "author": "Ramazan GUNINDI",
  "license": "ISC",
  "dependencies": {
    "lru-cache": "^11.5.2",
    "object-hash": "^3.0.0",
    "parse": "^8.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rgunindi/parse-cache-memory.git"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-jest": "^27.9.0",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "lint-staged": "^17.5.1",
    "prettier": "^3.2.5",
    "express": "^5.2.1",
    "parse-server": "^9.7.0",
    "mongodb-memory-server": "^11.2.0",
    "cross-env": "^10.1.0"
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix",
    "*.{js,json,md}": "prettier --write"
  },
  "files": [
    "index.js",
    "scripts/cleanup.js"
  ]
}
